version: 1
services:
  - type: web
    name: your-app-name
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python download_spacy_model.py
    startCommand: gunicorn app:app
